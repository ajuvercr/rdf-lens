import { defineConfig } from "vite";
import pack from './package.json'

export default defineConfig({
    // Library mode configuration
    build: {
        lib: {
            entry: "src/index.js",
            name: "rdf-lens-esm",
            fileName: "rdf-lens-" + pack.version,
            formats: ["es"] // ESM format for modern usage
        },
        outDir: "dist",
        emptyOutDir: false,
        sourcemap: false, // Disable for smaller size
        minify: false, // Use default minifier
        target: "es2020", // Modern browser support
    },
});
